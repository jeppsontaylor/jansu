# Copilot Instructions

Use `AGENTS.md` as the source of truth for agent behavior. It points to `MASTER_PLAN.md`, `AUDIT.md`, `tips/phases`, and `phase-logs`.

Do not suggest compatibility claims unless they are backed by `docs/compatibility/kafka-4.2-ledger.json` and tests.
